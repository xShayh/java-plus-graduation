CREATE TABLE IF NOT EXISTS users (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY KEY,
    name varchar(250) NOT NULL,
    email varchar(250) NOT NULL UNIQUE
);

CREATE TABLE IF NOT EXISTS events (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY KEY,

);